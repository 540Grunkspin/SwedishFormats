<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="org.grunkspin.testapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity
            android:name=".LongPersonalNumberActivity"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".ShortPersonalNumberActivity"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".MeterIdActivity"
            android:theme="@style/AppTheme"/>
        <activity
            android:name=".PostalCodeActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".ForcedPrefixMeterIdActivity"/>
    </application>
</manifest>
